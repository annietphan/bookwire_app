<h1 class="current-genre"><%= params[:genre] %></h1>

<% if @books.count == 0 %>
  <h1>There are no books in this genre.</h1>
<% else %>
  <% @books.each do |book| %>
    <h2><%= link_to book.title, book_path(book) %></h2>
  <% end %>
<% end %>
